# Manual improvements, corrections, and refactors

- Updated variable names for readability (e.g., `filtered` instead of `results`).
- Kept sample data small and deterministic to simplify testing.
- Added basic CSS variables and layout tweaks for better spacing and accessibility.
- Ensured components are small and focused (`FlightCard`) and moved markup into a components folder.
- Added descriptive README and linked files for easy review.
