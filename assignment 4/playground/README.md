# ARIA Playground

This playground contains three handcrafted accessible components (Modal/Dialog, Tabs, Disclosure) implemented in React + TypeScript following WAI-ARIA Authoring Practices.

Run:

```bash
cd "playground"
npm install
npm run dev
```

Open http://localhost:5173
